model:
  init_args:
    image_key: sentinel_2_l2a
    label_key: dynamicworld
    mask_key: cloud_mask
    ignore_index: 255
    class_map:
      0: water
      1: trees
      2: grass
      3: flooded_vegetation
      4: crops
      5: shrub_and_scrub
      6: built
      7: bare
    color_map:
      0: "#419bdf"
      1: "#397d49"
      2: "#88b053"
      3: "#7a87c6"
      4: "#e49635"
      5: "#dfc35a"
      6: "#c4281b"
      7: "#a59b8f"
    # trimmed to 3 bands (RGB) for smoke test only — matches encoder's
    # default 3-channel ImageNet mean/std. Restore full band_map + set
    # model.init_args.mean_norm/std_norm (len == band count) for real training.
    band_map:
      0: "B04"
      1: "B03"
      2: "B02"
      # 3: "B05"
      # 4: "B06"
      # 5: "B07"
      # 6: "B08"
      # 7: "B11"
      # 8: "B12"